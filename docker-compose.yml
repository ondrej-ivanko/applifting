version: '3'

services:
  web:
    build: .
    command: sh -c "gunicorn applifting.wsgi --bind 0.0.0.0:8080"
    container_name: web
    volumes:
      - .:/web
    ports:
      - "8080:8080"
    depends_on: 
      - "db"

  db:
    image: postgres:12.4-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=catalog
    ports: 
      - "5432:5432"

volumes:
  postgres_data: